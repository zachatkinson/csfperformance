<div class="{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %} color-{{ section.settings.color_scheme }}">
    <div class="{% if section.settings.include_margins %}page-width{% endif %}">
        <h2>Our Story</h2>
        <p>Our story dates back almost 80 years to the beginnings of a small radiator repair shop. Since then, four generations later and still family owned, CSF has expanded across the world while staying true to our roots.</p>
        <p>The Racing & High Performance division was founded to supply performance enthusiasts (just like ourselves) and professional racers a product that truly performs – proven in some of the most demanding races in motorsports with many world records.</p>
    </div>
</div>
{% schema %}
{
  "name": "Our Story CTA",
  "settings": [
    {
      "type": "checkbox",
      "id": "include_margins",
      "default": true,
      "label": "t:sections.featured-youtube.settings.include_margins.label"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    }
  ],
  "presets": [
    {
      "name": "Our Story CTA"
    }
  ],
  "blocks": [
    {
      "type": "story_cta",
      "name": "Our Story CTA",
      "settings": [
        {
          "type": "checkbox",
          "id": "include_margins",
          "default": true,
          "label": "t:sections.featured-youtube.settings.include_margins.label"
        },
        {
          "type": "color_scheme",
          "id": "color_scheme",
          "label": "t:sections.all.colors.label",
          "default": "scheme-1"
        }
      ]
    }
  ]
}
{% endschema %}
{% stylesheet %}
{% endstylesheet %}
{% javascript %}
{% endjavascript %}